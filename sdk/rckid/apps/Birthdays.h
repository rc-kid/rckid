#pragma once

namespace rckid {

    // TODO this should go to PIM module of the SDK, the bday app is 

    /** Contact information
     
        Each contact has a name and can have other properties as well, like birthday, phone number, telegram id, etc. 


     */
    class Contact {

    }; // rkid::Contact


    /** A simple app that manages contacts & their birthdays. 
     
        The birthdays app also dubs as a contact manager. 
     
     */
    class Birthdays {
    public:

    }; // rckid::Birthdays

} // namespace rckid